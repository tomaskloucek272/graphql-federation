type Order @key(fields: "id") {
    id: ID!
    clientId: ID!
    total: Float!
    status: String!
    createdAt: String
}

type Client @key(fields: "id") @extends {
    id: ID @external
    orders: [Order]
}

type Query {
    order(id: ID!): Order
}